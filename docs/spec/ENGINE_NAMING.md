# ENGINE NAMING — FROZEN v0.1

Canonical engine family:

```text
ELECTRONIC
BODY
MACHINE
```

Brand concept:

```text
EBM = ELECTRONIC BODY MACHINE
```

Canonical internal IDs:

```text
electronic
body
machine
```

Canonical code/path naming:

```text
ElectronicEngine
BodyEngine
MachineEngine

src/engines/electronic/
src/engines/body/
src/engines/machine/
```

No alias or compatibility layer is required for the retired pre-implementation
name because no released implementation exists yet.

If a task introduces any alternative engine name, stop and report a blocker.
